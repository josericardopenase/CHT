using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class clicker_controller : MonoBehaviour
{
    /*
     * 
     * clicker_controller:
     * controlador del dinero ganado a travez de diferentes clicks
     * 
     */

    // Start is called before the first frame update
    void Start()
    {
 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
